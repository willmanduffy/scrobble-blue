{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "bio-now-playing-text": {
      "type": "string",
      "description": "The text to display as the prefix for the now playing track in the Bluesky bio",
      "default": "🎵 Now playing: "
    }
  }
}
